<resources>
    <!-- Only used for the launch window; screens pick light/dark in BaseActivity. -->
    <style name="AppTheme" parent="android:Theme.DeviceDefault.Light.NoActionBar">
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>
